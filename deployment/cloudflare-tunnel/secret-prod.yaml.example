# このファイルはテンプレートです。実際のSecretは手動で作成してください。
#
# 手順:
# 1. Cloudflare Tunnelを作成
#    cloudflare tunnel create my-k3s-tunnel
#
# 2. credentials.jsonとconfig-prod.yamlを取得
#
# 3. Secretを作成
#    kubectl create secret generic cloudflared-secret \
#      --from-file=credentials.json=<path-to-credentials.json> \
#      --from-file=config.yaml=config-prod.yaml \
#      -n app
#
# 4. Cloudflare DNSでCNAMEレコード追加
#    api.octomblog.com -> <TUNNEL_ID>.cfargotunnel.com

apiVersion: v1
kind: Secret
metadata:
  name: cloudflared-secret
  namespace: app
type: Opaque
data:
  # credentials.json と config.yaml をbase64エンコードして設定
  # credentials.json: <base64-encoded-credentials>
  # config.yaml: <base64-encoded-config>
